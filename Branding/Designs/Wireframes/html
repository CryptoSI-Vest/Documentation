<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CryptoSI Vest Dashboard</title>
    <style>
        /* Global Styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, sans-serif;
        }

        body {
            background-color: #eaeaea; /* Light Gray */
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        /* HEADER */
        .header {
            width: 100%;
            max-width: 1440px;
            background-color: #252a34; /* Dark Gray */
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px;
        }

        .logo {
            font-size: 24px;
            font-weight: bold;
            color: #ffffff; /* White */
        }

        .nav-links {
            display: flex;
            gap: 20px;
        }

        .nav-links a {
            text-decoration: none;
            color: #ffffff; /* White */
            font-weight: bold;
            transition: color 0.3s;
        }

        .nav-links a:hover {
            color: #3d79fb; /* Primary Blue */
        }

        .wallet-button {
            background-color: #3d79fb; /* Primary Blue */
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-weight: bold;
            transition: background 0.3s;
        }

        .wallet-button:hover {
            background-color: #0b56fa; /* Secondary Blue */
        }

        /* MAIN LAYOUT */
        .container {
            display: flex;
            width: 100%;
            max-width: 1200px;
            margin-top: 20px;
        }

        .left-column {
            width: 70%;
            padding: 20px;
        }

        .right-column {
            width: 30%;
            padding: 20px;
            background-color: #ffffff; /* White */
            border-radius: 10px;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
        }

        /* STAT BOXES */
        .stats {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 10px;
            margin-bottom: 20px;
        }

        .stat-box {
            background-color: #ffffff; /* White */
            padding: 20px;
            text-align: center;
            border: 2px solid #3d79fb; /* Primary Blue */
            font-weight: bold;
            border-radius: 10px;
            color: #252a34; /* Dark Gray */
        }

        /* TABLE */
        .vesting-table {
            width: 100%;
            border-collapse: collapse;
            background-color: #ffffff; /* White */
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
        }

        .vesting-table th, .vesting-table td {
            border: 1px solid #ddd;
            padding: 10px;
            text-align: center;
            color: #252a34; /* Dark Gray */
        }

        .vesting-table th {
            background-color: #3d79fb; /* Primary Blue */
            color: white;
        }

        /* GRAPH PLACEHOLDER */
        .graph {
            width: 100%;
            height: 200px;
            background-color: white;
            text-align: center;
            line-height: 200px;
            margin-top: 20px;
            border-radius: 10px;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
            border: 2px solid #3d79fb; /* Primary Blue */
            color: #252a34;
        }

        /* RECENT TRANSACTIONS */
        .transactions, .quick-actions {
            background-color: white;
            padding: 20px;
            margin-bottom: 10px;
            border-radius: 10px;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
            border-left: 5px solid #3d79fb; /* Highlight */
        }

        /* FOOTER */
        .footer {
            width: 100%;
            max-width: 1440px;
            text-align: center;
            padding: 20px;
            background-color: #252a34; /* Dark Gray */
            color: white;
            margin-top: 20px;
        }

        /* BUTTON STYLES */
        .action-button {
            width: 100%;
            padding: 10px;
            background-color: #3d79fb; /* Primary Blue */
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-weight: bold;
            transition: background 0.3s;
            margin-top: 10px;
        }

        .action-button:hover {
            background-color: #0b56fa; /* Secondary Blue */
        }
    </style>
</head>
<body>

    <!-- HEADER -->
    <header class="header">
        <div class="logo">CryptoSI Vest</div>
        <nav class="nav-links">
            <a href="#">Dashboard</a>
            <a href="#">Create</a>
            <a href="#">Explore</a>
            <a href="#">Claim</a>
        </nav>
        <button class="wallet-button">Connect Wallet</button>
    </header>

    <!-- MAIN CONTAINER -->
    <div class="container">
        <!-- LEFT COLUMN -->
        <div class="left-column">
            <!-- Stats Boxes -->
            <div class="stats">
                <div class="stat-box">Total Vested: 100,000 XYZ</div>
                <div class="stat-box">Pending Tokens: 50,000 XYZ</div>
                <div class="stat-box">Eligible to Claim: 10,000 XYZ</div>
                <div class="stat-box">Price Target: $5.00</div>
            </div>

            <!-- Active Vesting Contracts Table -->
            <table class="vesting-table">
                <tr>
                    <th>Token Name</th>
                    <th>Vesting Type</th>
                    <th>Total Vested</th>
                    <th>Next Unlock</th>
                    <th>Target Price</th>
                    <th>Status</th>
                    <th>Details</th>
                </tr>
                <tr>
                    <td>XYZ Token</td>
                    <td>Hybrid</td>
                    <td>100,000 XYZ</td>
                    <td>Feb 15</td>
                    <td>$5.00</td>
                    <td>Locked</td>
                    <td><button class="action-button">View</button></td>
                </tr>
            </table>

            <!-- Vesting Progress Graph -->
            <div class="graph">[Vesting Progress Graph]</div>
        </div>

        <!-- RIGHT COLUMN -->
        <div class="right-column">
            <div class="transactions">
                <h3>Recent Transactions</h3>
                <p>Feb 1: 5,000 XYZ Released</p>
            </div>

            <div class="quick-actions">
                <h3>Quick Actions</h3>
                <button class="action-button">Create Vesting</button>
                <button class="action-button">Claim Tokens</button>
            </div>
        </div>
    </div>

    <footer class="footer">
        <p>Terms | Privacy | Support</p>
    </footer>

</body>
</html>
